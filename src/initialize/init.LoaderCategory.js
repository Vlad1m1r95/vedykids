import LoaderCategory from './../components/frontend/category/LoaderCategory'

const initLoaderCategory = (context, category, timer) => {
  LoaderCategory(context, category, timer)
}
export default initLoaderCategory